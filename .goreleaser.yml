project_name: go

before:
  hooks:
    - go mod tidy

builds:
  - skip: true

release:
  prerelease: auto
  github:
    owner: mockingio
    name: mockingio

changelog:
  use: github
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'